# ~/.gitconfig
[user]
	name = n45h4n
	email = n45r1.h4nd4l@gmail.com
[core]
	editor = nvim

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
